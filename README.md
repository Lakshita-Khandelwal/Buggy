# Buggy
Arduino-based autonomous buggy that follows a line, avoids obstacles, detects RF gantry checkpoints, and completes 2 clockwise laps — with full serial supervisory control and live status reporting at each checkpoint.
